name: first workflow
on: push

jobs:
    first_job:
            run-on: ubuntu-latest
            steps:
            - name: welcome message
              run: echo "hello world"
            - name: list directory
              run: ls 